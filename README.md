# SOL--DDL-COMMAND-CONSTRAINTS
This is covering DDL commands and database constraints, including CREATE, ALTER, RENAME, TRUNCATE, DROP, and key constraints.
**This repository  covering:**
Database and table creation
ALTER TABLE commands
Table renaming
TRUNCATE
DROP
PRIMARY KEY
NOT NULL
UNIQUE
AUTO_INCREMENT
CHECK
DEFAULT
FOREIGN KEY constraints
**File**
`MySQL_Assignment 1.sql` 
**Database**
Database name: `employee`
